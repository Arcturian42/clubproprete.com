/** Vue d'ensemble back-office (placeholder Phase 0 — KPI + files en MVP 1/2). */
export default function AdminPage() {
  return (
    <main className="mx-auto max-w-4xl px-4 py-8">
      <h1 className="text-h2 font-bold text-navy">Back-office</h1>
      <p className="mt-2 text-body text-grey">
        Vue d&apos;ensemble, utilisateurs &amp; capacités, entités, demandes, modération — à venir.
      </p>
    </main>
  );
}
